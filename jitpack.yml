jdk:
  - openjdk11
before_install:
  - chmod +x gradlew
  - curl -s "https://get.sdkman.io" | bash
  - source "$HOME/.sdkman/bin/sdkman-init.sh"
  - sdk install gradle 8.9
  - sdk use gradle 8.9
install:
  - gradle :bleSdk:assembleRelease --no-daemon